deployment:
  tasks:
    - export DEPLOYPATH=/home/markzpwz/public_html/Dit-Dah-Dash/
    # Make sure the source path points to your code subdirectory:
    - /bin/rsync -av --delete --exclude '.git' --exclude '.cpanel.yml' ./Dit-Dah-Dash_Refactored/ "${DEPLOYPATH}"
    - find "${DEPLOYPATH}" -type d -exec chmod 755 {} \;
    # - find "${DEPLOYPATH}" -type f -exec chmod 644 {} \;